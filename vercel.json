{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "cd escape-ad && npm install && npm run build",
  "outputDirectory": "escape-ad/dist",
  "framework": null,
  "rewrites": [{ "source": "/model-test", "destination": "/model-test.html" }]
}
